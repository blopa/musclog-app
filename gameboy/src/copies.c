/* copies.c: all user-visible strings for Musclog GB are defined as macros in copies.h.
   This file exists only so the build system includes it as a compilation unit. */
#include "copies.h"

/* Dummy declaration to satisfy ISO C "empty translation unit" requirement. */
typedef int copies_dummy_t;
