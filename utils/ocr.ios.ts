/**
 * iOS OCR Implementation
 *
 * TODO: implement iOS OCR - make sure to not add it to the web or Android bundle, to not break their build
 * find a package that works for iOS and use it
 */
export async function performOcr(imageUri: string): Promise<string | null> {
  console.warn(
    '[ocr.ios.ts] performOcr is not implemented for iOS.'
  );

  return null;
}
