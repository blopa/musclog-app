name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '26'

      - name: Cache node_modules and Expo cache
        id: cache-npm
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            .expo
            /tmp/metro-*
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        run: npm ci

      - name: Build web application
        env:
          NODE_ENV: production
        run: |
          npm run build-android-web
          touch dist/.nojekyll

      - name: Copy static images to dist
        run: |
          rm -rf dist/images dist/musclog-app/images
          mkdir -p dist/images dist/musclog-app/images
          cp -rL public/images/. dist/images/
          cp -rL public/musclog-app/images/. dist/musclog-app/images/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
