/**
 * Shared types for native health integrations (Health Connect + HealthKit).
 * Domain errors remain `HealthConnectError` for one i18n + snackbar path.
 */
export {
  HealthConnectError,
  HealthConnectErrorCode,
  HealthConnectErrorFactory,
} from '../healthConnectErrors';
